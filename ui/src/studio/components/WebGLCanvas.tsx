/**
 * WebGL Node Graph Canvas — Phase 4
 *
 * Replaces React Flow with a custom WebGL renderer.
 * Nodes are GPU quads. Cables are Bezier curves with animated signal flow.
 * Handles 1000+ nodes at 60fps.
 */

export { WebGLCanvas } from "./WebGLCanvasImpl";
