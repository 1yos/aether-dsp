/**
 * PatcherView — the node graph (formerly Create mode).
 * Advanced sound design view. Most users won't need this.
 */

import { StudioCanvas } from "../../studio/components/StudioCanvas";

export function PatcherView() {
  return <StudioCanvas />;
}
